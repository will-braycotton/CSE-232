#ifndef PLACE_HOLDER
#define PLACE_HOLDER

#include <vector>
using std::vector;

const int *  FindSmallestIndexRange( const vector<int> &v, int first, int last );

//template<typename T>
/*void DoubleIteratorRange( std::vector<float>::iterator ptr,std::vector<float>::iterator last ){
    for( ptr; ptr != last; ++ptr){
       *ptr = (*ptr) * 2;
    }




}
*/

#endif