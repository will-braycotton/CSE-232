#pragma once
#include <string>
#include <vector>

class Penultimate{
    private:
    std::vector<std::string> main;

    public:
        Penultimate(std::string);
        std::string push_back(std::string);
};